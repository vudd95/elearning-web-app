type IActiveLinkProps = {
  url: string;
  children: React.ReactNode;
};
type IMenuItemProps = {
  url: string;
  title: string;
  icon: React.ReactNode;
};

export { IActiveLinkProps, IMenuItemProps };
